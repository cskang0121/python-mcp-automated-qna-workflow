# Google Sheets Configuration
GOOGLE_SHEETS_ID=your-google-sheet-id-here
GOOGLE_CREDENTIALS_PATH=./google-credentials.json

# Optional: Specific sheet name/tab (defaults to first sheet)
SHEET_NAME=Sheet1

# Optional: Range to work with (defaults to all data)
SHEET_RANGE=A:F